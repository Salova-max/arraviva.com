server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # URLs limpias: /cuestionarios/dental sirve dental.html, /privacidad sirve privacidad.html, etc.
    location / {
        try_files $uri $uri.html $uri/ =404;
    }
}
